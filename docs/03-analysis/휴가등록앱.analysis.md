# 휴가등록앱 Analysis Report

> **Analysis Type**: Gap Analysis (Design vs Implementation)
>
> **Project**: 휴가등록앱
> **Version**: 0.1.0
> **Analyst**: gap-detector (bkit v1.5.3)
> **Date**: 2026-02-12
> **Design Doc**: [휴가등록앱.design.md](../02-design/features/휴가등록앱.design.md)

### Pipeline References (for verification)

| Phase | Document | Verification Target |
|-------|----------|---------------------|
| Phase 2 | Inline (Design Doc Section 10) | Convention compliance |
| Phase 4 | Design Doc Section 4 | API implementation match |
| Phase 8 | This document | Architecture/Convention review |

---

## 1. Analysis Overview

### 1.1 Analysis Purpose

Design document (`docs/02-design/features/휴가등록앱.design.md`) 와 실제 구현 코드 간의 불일치(gap)를 식별하고, Match Rate를 계산하여 PDCA Check 단계를 완료한다.

### 1.2 Analysis Scope

- **Design Document**: `docs/02-design/features/휴가등록앱.design.md`
- **Implementation Paths**:
  - Backend: `app/main.py`, `app/models.py`, `app/routes/auth.py`, `app/routes/leaves.py`, `app/services/bkend.py`
  - Frontend: `app/templates/*.html`, `app/templates/partials/*`
  - Static: `static/css/style.css`, `static/js/calendar.js`
  - Config: `requirements.txt`, `.env`, `.env.example`, `.gitignore`
- **Analysis Date**: 2026-02-12

---

## 2. Gap Analysis (Design vs Implementation)

### 2.1 API Endpoints

| Design | Implementation | Status | Notes |
|--------|---------------|--------|-------|
| GET `/` (Calendar View, Auth Required) | GET `/` in `leaves.py` | ⚠️ Partial | No auth guard implemented |
| GET `/login` | GET `/login` in `auth.py` | ✅ Match | |
| POST `/login` | POST `/login` in `auth.py` | ⚠️ Partial | bkend.ai auth NOT implemented (TODO stub) |
| GET `/signup` | GET `/signup` in `auth.py` | ✅ Match | |
| POST `/signup` | POST `/signup` in `auth.py` | ⚠️ Partial | bkend.ai user creation NOT implemented (TODO stub) |
| GET `/logout` | GET `/logout` in `auth.py` | ⚠️ Partial | Session/token deletion NOT implemented (TODO) |
| GET `/leaves` | GET `/leaves` in `leaves.py` | ⚠️ Partial | No `month`, `limit`, `offset` query params in route |
| GET `/leaves/new` | GET `/leaves/new` in `leaves.py` | ✅ Match | |
| POST `/leaves` | POST `/leaves` in `leaves.py` | ✅ Match | Date validation + bkend.ai call present |
| GET `/leaves/{id}` | GET `/leaves/{leave_id}` in `leaves.py` | ✅ Match | |
| POST `/leaves/{id}/edit` | POST `/leaves/{leave_id}/edit` in `leaves.py` | ✅ Match | |
| POST `/leaves/{id}/delete` | POST `/leaves/{leave_id}/delete` in `leaves.py` | ✅ Match | |
| - | GET `/health` in `main.py` | ⚠️ Added | Not in design (acceptable for ops) |

**Endpoint Match Rate**: 7/12 full match = 58%, 5/12 partial = 42%

### 2.2 Data Model

| Field | Design Type | Impl Type | Status | Notes |
|-------|-------------|-----------|--------|-------|
| `id` | `str \| None` | `str \| None` | ✅ Match | |
| `user_id` | `str` | `str` | ✅ Match | |
| `leave_type` | `LeaveType` (Enum) | `LeaveType` (Enum) | ✅ Match | |
| `start_date` | `date` | `date` | ✅ Match | |
| `end_date` | `date` | `date` | ✅ Match | |
| `reason` | `str \| None` (max 500) | `str \| None` (max 500) | ✅ Match | |
| `created_at` | `str \| None` | `str \| None` | ✅ Match | |
| `updated_at` | `str \| None` | `str \| None` | ✅ Match | |

**LeaveType Enum Values**:

| Design Value | Impl Value | Status |
|-------------|-----------|--------|
| 년가 (ANNUAL) | 년가 (ANNUAL) | ✅ Match |
| 지참 (HALF_DAY) | 지참 (HALF_DAY) | ✅ Match |
| 조퇴 (EARLY_LEAVE) | 조퇴 (EARLY_LEAVE) | ✅ Match |
| 외출 (OUTING) | 외출 (OUTING) | ✅ Match |
| 병가 (SICK) | 병가 (SICK) | ✅ Match |
| 공가 (PUBLIC) | 공가 (PUBLIC) | ✅ Match |

**Pydantic Models**:

| Model | Design | Implementation | Status |
|-------|--------|---------------|--------|
| `Leave` | Defined | Defined | ✅ Match |
| `LeaveCreateRequest` | Defined | Defined with `validate_dates()` | ✅ Match (enhanced) |
| `LeaveUpdateRequest` | Defined | Defined | ✅ Match |

**Data Model Match Rate**: 100%

### 2.3 File Structure

| Design Path | Implementation | Status | Notes |
|------------|----------------|--------|-------|
| `app/main.py` | Exists | ✅ Match | |
| `app/models.py` | Exists | ✅ Match | |
| `app/routes/__init__.py` | Exists (empty) | ✅ Match | |
| `app/routes/auth.py` | Exists | ✅ Match | |
| `app/routes/leaves.py` | Exists | ✅ Match | |
| `app/services/__init__.py` | Exists (empty) | ✅ Match | |
| `app/services/bkend.py` | Exists | ✅ Match | |
| `app/templates/base.html` | Exists | ✅ Match | |
| `app/templates/login.html` | Exists | ✅ Match | |
| `app/templates/signup.html` | Exists | ✅ Match | |
| `app/templates/calendar.html` | Exists | ✅ Match | |
| `app/templates/list.html` | Exists | ✅ Match | |
| `app/templates/new_leave.html` | Exists | ✅ Match | |
| `app/templates/edit_leave.html` | Exists | ✅ Match | |
| `app/templates/partials/header.html` | **NOT FOUND** | ❌ Missing | Design requires partial templates |
| `app/templates/partials/footer.html` | **NOT FOUND** | ❌ Missing | Design requires partial templates |
| `app/templates/partials/leave_card.html` | **NOT FOUND** | ❌ Missing | Design requires partial templates |
| `app/templates/partials/calendar_day.html` | **NOT FOUND** | ❌ Missing | Design requires partial templates |
| `static/css/style.css` | Exists | ✅ Match | |
| `static/js/calendar.js` | Exists | ✅ Match | |
| `requirements.txt` | Exists | ✅ Match | |
| `.env` | Exists | ✅ Match | |
| `.gitignore` | Exists | ✅ Match | |

**CRITICAL**: `base.html` references `{% include "partials/header.html" %}` and `{% include "partials/footer.html" %}` at lines 18 and 26, but these files do not exist on disk. This will cause a **runtime TemplateNotFoundError** when the application starts.

**File Structure Match Rate**: 19/23 = 83%

### 2.4 UI Components & Templates

| Design Component | Implementation | Status | Notes |
|-----------------|---------------|--------|-------|
| Login page (`login.html`) | Exists with full form | ✅ Match | Username + password fields, signup link |
| Signup page (`signup.html`) | Exists with full form | ✅ Match | Username + email + password, login link |
| Calendar view (`calendar.html`) | Exists with static grid | ⚠️ Partial | Hardcoded "2026년 2월", no dynamic month navigation |
| Leave list (`list.html`) | Exists with table layout | ✅ Match | Full table with edit/delete actions |
| New leave form (`new_leave.html`) | Exists with full form | ✅ Match | All 6 leave types, date validation, char counter |
| Edit leave form (`edit_leave.html`) | Exists with full form | ✅ Match | Pre-filled values, delete button |
| Header partial (`partials/header.html`) | **NOT FOUND** | ❌ Missing | Referenced in base.html but does not exist |
| Footer partial (`partials/footer.html`) | **NOT FOUND** | ❌ Missing | Referenced in base.html but does not exist |
| Leave card partial (`partials/leave_card.html`) | **NOT FOUND** | ❌ Missing | Designed for leave list items |
| Calendar day partial (`partials/calendar_day.html`) | **NOT FOUND** | ❌ Missing | Designed for calendar date cells |
| Error page (`error.html`) | **NOT FOUND** | ❌ Missing | Referenced in `leaves.py` lines 40, 59 but does not exist |

**UI Match Rate**: 6/11 = 55%

### 2.5 Match Rate Summary

```
+-----------------------------------------------+
|  Overall Match Rate: 72%                       |
+-----------------------------------------------+
|  Data Model:         100%  (8/8 fields)        |
|  API Endpoints:       58%  (7/12 full match)   |
|  File Structure:      83%  (19/23 files)       |
|  UI Components:       55%  (6/11 components)   |
|  Service Layer:       90%  (CRUD implemented)   |
|  Config/Env:          85%  (vars present)       |
+-----------------------------------------------+
```

---

## 3. Detailed Gap Findings

### 3.1 Missing Features (Design O, Implementation X)

| # | Item | Design Location | Description | Severity |
|---|------|-----------------|-------------|----------|
| M-01 | Partial templates (header, footer, leave_card, calendar_day) | Design Section 5.4 | 4 Jinja2 partial templates defined in design are not created. `base.html` includes `partials/header.html` and `partials/footer.html` which will cause runtime errors. | CRITICAL |
| M-02 | bkend.ai Authentication | Design Section 7 | Login/signup routes are stubs with TODO comments. No actual bkend.ai JWT token auth. `auth.py` line 34: `if username and password: return redirect` -- accepts any input. | HIGH |
| M-03 | Session/Token Management | Design Section 4.1 | No session middleware, no cookie-based token storage. `get_current_user_id()` in `leaves.py` returns hardcoded `"user123"`. | HIGH |
| M-04 | Auth Guard (Route Protection) | Design Section 4.1 (Auth Required on 7 endpoints) | No authentication middleware or dependency injection guard. All protected routes are accessible without login. | HIGH |
| M-05 | Error page template | `leaves.py` lines 40, 59 | Code references `error.html` template which does not exist -- will cause `TemplateNotFoundError`. | CRITICAL |
| M-06 | Query parameters on GET `/leaves` | Design Section 4.3 | Design specifies `month`, `limit`, `offset` query parameters. Route implementation does not accept these. | MEDIUM |
| M-07 | Calendar month navigation (JavaScript) | Design Section 5.2, `calendar.js` | `navigateMonth()`, `showLeaveDetail()`, `filterByLeaveType()` are all empty stubs with TODO. Prev/Next buttons in `calendar.html` have no `onclick` handlers. | MEDIUM |
| M-08 | Leave type color coding in calendar | Design Section 5.1, `style.css` | CSS classes `.leave-annual`, `.leave-halfday`, etc. are defined but NOT used in `calendar.html`. All leave markers use hardcoded `bg-blue-100 text-blue-700`. | LOW |
| M-09 | CSRF protection middleware | Design Section 7 | Design specifies "FastAPI CSRF middleware applied for POST requests". No CSRF middleware found in `main.py`. | MEDIUM |
| M-10 | Logging | Design Section 6.3 (Error Handling Pattern) | Design shows `logger.error()` and `logger.warning()` usage. No logging configuration or logger imports in implementation. | MEDIUM |

### 3.2 Added Features (Design X, Implementation O)

| # | Item | Implementation Location | Description | Impact |
|---|------|------------------------|-------------|--------|
| A-01 | Health check endpoint | `app/main.py` line 30 | `GET /health` returns app status. Not in design. | None (positive addition) |
| A-02 | APP_NAME env var | `.env` line 11 | `APP_NAME` and `APP_VERSION` environment variables not in design Section 10.3. | None (acceptable) |
| A-03 | `.env.example` file | `.env.example` | Template file for env vars exists but not listed in design file structure. | None (best practice) |
| A-04 | Client-side date validation | `new_leave.html` line 109-116 | JavaScript date validation in browser. Design only specifies server-side validation. | None (positive UX) |
| A-05 | Character counter | `new_leave.html` line 97-103 | Real-time character counting for reason field. | None (positive UX) |

### 3.3 Changed/Inconsistent Features (Design != Implementation)

| # | Item | Design | Implementation | Impact |
|---|------|--------|----------------|--------|
| C-01 | Calendar hardcoded month | Dynamic monthly view | `calendar.html` line 19: hardcoded "2026년 2월", `range(1, 31)` fixed 30 days | HIGH - Calendar is static, not functional |
| C-02 | Calendar leave day matching | Date-range based display | `calendar.html` line 41: uses `.day` property only (ignores month/year), will show incorrect matches across months | HIGH - Logic bug |
| C-03 | Error response format | Design Section 6.2: `{ "error": { "code": "INVALID_INPUT", "message": "..." } }` | `leaves.py`: Uses FastAPI default `{ "detail": "message" }` via HTTPException | MEDIUM - API contract mismatch |
| C-04 | GET `/leaves` response format | Design Section 4.3: `{ "items": [...], "total": N, "limit": 50, "offset": 0 }` | Returns HTML (template rendering), no JSON pagination response | LOW - HTML app, not pure API |
| C-05 | `update_leave` parameter type | Design Section 3.1: `LeaveUpdateRequest` model | `leaves.py` line 137: uses raw `dict` instead of Pydantic model | LOW - Functional but less type-safe |
| C-06 | edit_leave.html nested form | Single form per page | `edit_leave.html` line 83: nested `<form>` inside outer `<form>` -- invalid HTML, delete button may not work properly | MEDIUM - HTML structure bug |
| C-07 | Stats "이번 달" / "다음 달" | Dynamic calculation | `calendar.html` lines 88, 93: hardcoded `0`, not calculated from leave data | LOW - Cosmetic |
| C-08 | `BkendService` singleton init | Lazy or conditional init | `bkend.py` line 166: `bkend_service = BkendService()` at module import time. Will raise `ValueError` immediately if `.env` is missing credentials, preventing app startup even for health checks. | MEDIUM - Startup reliability |

---

## 4. Code Quality Analysis

### 4.1 Code Smells

| Type | File | Location | Description | Severity |
|------|------|----------|-------------|----------|
| Hardcoded value | `app/routes/leaves.py` | L23 | `return "user123"` -- hardcoded user ID | CRITICAL |
| TODO stubs | `app/routes/auth.py` | L27-30, L59-61, L76 | Auth logic is entirely placeholder | HIGH |
| TODO stubs | `static/js/calendar.js` | L11-16, L23, L31, L39 | All JS functions are empty stubs | MEDIUM |
| Nested form | `app/templates/edit_leave.html` | L83-90 | `<form>` nested inside another `<form>` | MEDIUM |
| Missing template | `app/routes/leaves.py` | L40, L59 | References `error.html` that does not exist | CRITICAL |
| Missing partials | `app/templates/base.html` | L18, L26 | References `partials/header.html` and `partials/footer.html` that do not exist | CRITICAL |
| Inconsistent typing | `app/services/bkend.py` | L5 | Uses `from typing import List, Optional` (old style) vs `list[]`, `str | None` (new style in models.py) | LOW |

### 4.2 Security Issues

| Severity | File | Location | Issue | Recommendation |
|----------|------|----------|-------|----------------|
| CRITICAL | `app/routes/auth.py` | L34 | No actual authentication -- any input accepted | Implement bkend.ai JWT auth |
| CRITICAL | `app/routes/leaves.py` | L23 | Hardcoded user_id bypasses all authorization | Implement session/token validation |
| HIGH | `.env` | L2-4 | `.env` contains `temp_key` credentials, committed to repo (`.gitignore` has `.env` but file exists) | Verify `.env` is not tracked in git |
| MEDIUM | `app/main.py` | - | No CSRF middleware configured | Add starlette-csrf or custom CSRF protection |
| LOW | `app/templates/*.html` | - | No CSP (Content Security Policy) headers | Add security headers middleware |

---

## 5. Test Coverage

### 5.1 Coverage Status

| Area | Current | Target | Status |
|------|---------|--------|--------|
| Unit Tests | 0% | Manual test only (Starter level) | N/A (Design specifies manual testing) |
| Integration Tests | 0% | Manual test only | N/A |
| Manual Test Cases | Not executed | 7 Happy Path + 3 Error + 3 Edge | Not verified |

> Note: Design document (Section 8) specifies Starter level = manual testing only. However, the application cannot currently run due to missing template files (partials/header.html, partials/footer.html, error.html), so manual testing is blocked.

---

## 6. Clean Architecture Compliance

> Reference: Design Document Section 9 (Starter Level)

### 6.1 Layer Structure Verification

| Layer | Design | Actual Location | Status | Notes |
|-------|--------|-----------------|--------|-------|
| Presentation | `templates/`, `static/` | `app/templates/`, `static/` | ✅ Match | |
| API Routes | `app/routes/` | `app/routes/auth.py`, `app/routes/leaves.py` | ✅ Match | |
| Business Logic | `app/services/` | `app/services/bkend.py` | ✅ Match | |
| Data Models | `app/models.py` | `app/models.py` | ✅ Match | |

### 6.2 Dependency Direction Verification

| Rule | Expected | Actual | Status |
|------|----------|--------|--------|
| Routes -> Services (only through services for bkend.ai) | Routes import services | `leaves.py` L5: `from app.services.bkend import bkend_service` | ✅ Correct |
| Routes -> Models | Routes import models | `leaves.py` L6: `from app.models import LeaveCreateRequest, LeaveType` | ✅ Correct |
| Services -> Models | Services import models | `bkend.py` L5: `from app.models import Leave, LeaveCreateRequest` | ✅ Correct |
| Templates use only passed data | No direct API calls from templates | Templates use `{{ }}` vars only | ✅ Correct |
| No circular dependencies | Unidirectional flow | Routes -> Services -> Models | ✅ Correct |

### 6.3 Architecture Score

```
+-----------------------------------------------+
|  Architecture Compliance: 95%                  |
+-----------------------------------------------+
|  Correct layer placement:  All files           |
|  Dependency direction:     Correct             |
|  Separation of concerns:   Good                |
|  Deduction:                                    |
|    -5% Templates include non-existent partials |
+-----------------------------------------------+
```

---

## 7. Convention Compliance

> Reference: Design Document Section 10 (PEP8 + FastAPI Best Practices)

### 7.1 Naming Convention Check

| Category | Convention | Files Checked | Compliance | Violations |
|----------|-----------|:------------:|:----------:|------------|
| Functions | snake_case | 16 functions | 100% | None |
| Classes | PascalCase | 5 classes | 100% | None |
| Constants | UPPER_SNAKE_CASE | -- | N/A | No constants defined (design mentions `MAX_REASON_LENGTH`, `API_BASE_URL` but not implemented) |
| Variables | snake_case | All files | 100% | None |
| Files (Python) | snake_case.py | 5 files | 100% | None |
| Templates | snake_case.html | 7 files | 100% | None |
| Folders | snake_case | `routes/`, `services/`, `templates/`, `partials/` | 100% | None |

### 7.2 Import Order Check

**`app/main.py`**:
```
1. fastapi (third-party)         -- should be after stdlib
2. fastapi.staticfiles           -- third-party
3. fastapi.templating            -- third-party
4. dotenv (third-party)          -- third-party
5. os (stdlib)                   -- VIOLATION: stdlib should come first
6. app.routes (local)            -- correct position
```
Status: **VIOLATION** -- `os` import at line 6 comes after third-party imports at lines 1-4.

**`app/routes/leaves.py`**:
```
1. fastapi (third-party)
2. fastapi.responses
3. fastapi.templating
4. app.services.bkend (local)
5. app.models (local)
6. datetime (stdlib)             -- VIOLATION: stdlib should come first
```
Status: **VIOLATION** -- `datetime` import at line 7 comes after third-party and local imports.

**`app/services/bkend.py`**:
```
1. httpx (third-party)
2. os (stdlib)                   -- VIOLATION: stdlib should come first
3. typing (stdlib)               -- VIOLATION: stdlib should come first
4. app.models (local)
```
Status: **VIOLATION** -- `os` and `typing` come after third-party `httpx`.

**`app/routes/auth.py`**: Correct order (fastapi first, then no stdlib/local conflicts).

**`app/models.py`**: Correct order (pydantic, datetime, enum -- all appropriate).

### 7.3 Code Style Check

| Rule | Status | Violations |
|------|--------|------------|
| Type hints on functions | ⚠️ Partial | `auth.py` routes lack return type hints; `leaves.py` most routes lack return type hints |
| Docstrings on functions | ⚠️ Partial | `bkend.py` has full docstrings; `auth.py` has partial; `leaves.py` has brief ones |
| Error messages in Korean | ✅ Match | Consistent Korean error messages as per design |
| Max line length (PEP8: 79/120) | ✅ Match | No lines exceed reasonable length |

### 7.4 Environment Variable Check

| Variable | Design | .env | .env.example | Status |
|----------|--------|------|-------------|--------|
| `BKEND_API_KEY` | Required | `temp_key` | Template value | ✅ Present |
| `BKEND_PROJECT_ID` | Required | `temp_project` | Template value | ✅ Present |
| `BKEND_ENV_ID` | Required | `temp_env` | Template value | ✅ Present |
| `SECRET_KEY` | Required | Temp value | Template value | ✅ Present |
| `DEBUG` | Required | `True` | `True` | ✅ Present |
| `APP_NAME` | Not in design | `휴가등록앱` | `휴가등록앱` | ⚠️ Added (not in design) |
| `APP_VERSION` | Not in design | `0.1.0` | `0.1.0` | ⚠️ Added (not in design) |

### 7.5 Convention Score

```
+-----------------------------------------------+
|  Convention Compliance: 82%                    |
+-----------------------------------------------+
|  Naming:              100%                     |
|  Folder Structure:     83%                     |
|  Import Order:         40% (3/5 files wrong)   |
|  Code Style (hints):   70%                     |
|  Env Variables:        90%                     |
+-----------------------------------------------+
```

---

## 8. Overall Scores

| Category | Score | Status |
|----------|:-----:|:------:|
| Design Match (API + Data + UI) | 72% | ⚠️ |
| Architecture Compliance | 95% | ✅ |
| Convention Compliance | 82% | ⚠️ |
| **Overall** | **72%** | **⚠️** |

```
+-----------------------------------------------+
|  Overall Score: 72/100                         |
+-----------------------------------------------+
|  Data Model Match:     100 points              |
|  API Endpoint Match:    58 points              |
|  File Structure:        83 points              |
|  UI Components:         55 points              |
|  Architecture:          95 points              |
|  Convention:            82 points              |
|  Security:              30 points              |
|  Functionality:         50 points              |
+-----------------------------------------------+
|  Weighted Average:      72 points              |
+-----------------------------------------------+
```

---

## 9. Recommended Actions

### 9.1 Immediate (CRITICAL -- App Cannot Run)

| Priority | Item | File | Description |
|----------|------|------|-------------|
| C-1 | Create `partials/header.html` | `app/templates/partials/header.html` | `base.html` includes this file. Without it the app crashes on any page load with `TemplateNotFoundError`. |
| C-2 | Create `partials/footer.html` | `app/templates/partials/footer.html` | `base.html` includes this file. Same crash as above. |
| C-3 | Create `error.html` | `app/templates/error.html` | `leaves.py` references this in error handlers. Without it, errors will cause a secondary `TemplateNotFoundError`. |

### 9.2 High Priority (Core Functionality Broken)

| Priority | Item | File | Description |
|----------|------|------|-------------|
| H-1 | Implement bkend.ai authentication | `app/routes/auth.py` | Replace stub login/signup with actual bkend.ai API calls and JWT token handling. |
| H-2 | Implement session/token management | `app/routes/leaves.py` | Replace `get_current_user_id()` hardcoded `"user123"` with real session/token validation from cookies. |
| H-3 | Add auth guard middleware/dependency | `app/main.py` or `app/routes/` | Create FastAPI dependency for route protection. 7 endpoints require authentication per design. |
| H-4 | Fix nested form in `edit_leave.html` | `app/templates/edit_leave.html` L83 | Nested `<form>` inside `<form>` is invalid HTML. Extract delete button form outside the edit form. |

### 9.3 Medium Priority (Functionality Gaps)

| Priority | Item | File | Expected Impact |
|----------|------|------|-----------------|
| M-1 | Implement calendar month navigation | `static/js/calendar.js`, `app/templates/calendar.html` | Make calendar functional with prev/next month, dynamic date rendering |
| M-2 | Add month/limit/offset params to GET `/leaves` | `app/routes/leaves.py` | Match API design specification |
| M-3 | Apply leave type color coding in calendar | `app/templates/calendar.html` | Use CSS classes from `style.css` instead of hardcoded blue |
| M-4 | Add CSRF protection middleware | `app/main.py` | Security requirement from design |
| M-5 | Add logging configuration | `app/main.py` | Design specifies `logger.error()` and `logger.warning()` usage |
| M-6 | Fix import order (PEP8) | `app/main.py`, `app/routes/leaves.py`, `app/services/bkend.py` | Move stdlib imports before third-party imports |
| M-7 | Standardize error response format | `app/routes/leaves.py` | Use `{ "error": { "code": "...", "message": "..." } }` format per design Section 6.2 |
| M-8 | Fix `BkendService` singleton init | `app/services/bkend.py` | Add lazy initialization or try/except to prevent crash when env vars missing |

### 9.4 Low Priority (Polish / Enhancement)

| Item | File | Notes |
|------|------|-------|
| Create `partials/leave_card.html` | `app/templates/partials/` | Reusable leave card component for list view |
| Create `partials/calendar_day.html` | `app/templates/partials/` | Reusable calendar day cell component |
| Calculate "이번 달" / "다음 달" stats | `app/templates/calendar.html` | Dynamic stat calculation from leave data |
| Add type hints to all route functions | `app/routes/auth.py`, `leaves.py` | PEP8 best practice |
| Replace `typing.List`, `typing.Optional` with modern syntax | `app/services/bkend.py` | Use `list[]` and `X \| None` (Python 3.10+) |
| Define named constants | `app/models.py` or new `constants.py` | `MAX_REASON_LENGTH = 500`, etc. per design convention |

---

## 10. Design Document Updates Needed

The following items exist in implementation but are not documented in the design:

- [ ] Add `GET /health` endpoint to API specification (Section 4.1)
- [ ] Add `APP_NAME` and `APP_VERSION` to environment variables (Section 10.3)
- [ ] Add `.env.example` to file structure (Section 9.3)
- [ ] Document client-side date validation behavior (Section 6)

---

## 11. Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| App crashes on startup (missing partials) | Certain | CRITICAL | Create header.html, footer.html immediately |
| App crashes on error path (missing error.html) | High | HIGH | Create error.html template |
| Unauthorized access to all leave data | Certain (current state) | CRITICAL | Implement authentication before deployment |
| Data leakage (all users see user123 data) | Certain | HIGH | Remove hardcoded user_id |
| Calendar shows wrong leave data | High | MEDIUM | Fix day-only matching logic |

---

## 12. Next Steps

Based on Match Rate of **72%** (below 90% threshold):

- [ ] **PRIORITY 1**: Fix CRITICAL items (C-1, C-2, C-3) -- app cannot even start
- [ ] **PRIORITY 2**: Implement authentication (H-1, H-2, H-3) -- core security
- [ ] **PRIORITY 3**: Fix HTML structure bug (H-4)
- [ ] **PRIORITY 4**: Complete calendar functionality (M-1, M-3)
- [ ] **PRIORITY 5**: Add middleware and logging (M-4, M-5)
- [ ] **PRIORITY 6**: Fix code style issues (M-6)
- [ ] **PRIORITY 7**: Update design document (Section 10)
- [ ] Run `/pdca iterate 휴가등록앱` after fixes to re-verify (target: >= 90%)

**Recommendation**: Match Rate < 70% threshold for "significant gap" is nearly reached. A focused iteration fixing CRITICAL and HIGH items should bring the score above 85%. A second iteration addressing MEDIUM items should achieve the 90% target.

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-12 | Initial gap analysis | gap-detector |
